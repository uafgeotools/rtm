name: rtm
channels:
  - conda-forge
  - defaults
dependencies:
  - cartopy
  - ipython
  - jupyterlab
  - obspy
  - pip
#  - pygmt  # OPTIONAL (needed for infresnel)
  - rioxarray
  - tqdm
  - pip:
    - git+https://github.com/uafgeotools/waveform_collection.git
#    - git+https://github.com/liamtoney/infresnel.git  # OPTIONAL
    - --editable .
