name: rtm
channels:
  - conda-forge
  - defaults
dependencies:
  - cartopy
  - gdal
  - ipython
  - numpy<1.22  # TEMPORARY
  - obspy
  - pip
  - utm
  - xarray
  - pip:
    - git+https://github.com/uafgeotools/waveform_collection.git
    - --editable .
