name: rtm
channels:
  - conda-forge
  - defaults
dependencies:
  - cartopy
  - gdal
  - ipython
  - jupyterlab
  - obspy
  - pip
  - tqdm
  - utm
  - xarray
  - pip:
    - git+https://github.com/uafgeotools/waveform_collection.git
    - --editable .
